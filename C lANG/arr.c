#include<stdio.h>

structure student{
    int rnum;
    char n[10];
    int mark[5];
}

void main(){
    int i,r[10],m[10];

    struct student s;
    printf("enter num: ");
    scanf("%d",s.rnum);
    printf("enter name: ");
    scanf("%s",s.n[10]);
    printf("enter mark: ");
    
    for ( i = 0; i < 10; i++)
    {
        scanf("%d",&)s.mark[5];
        total = total + mark[i];
        
    }
    printf("%d",total);
}